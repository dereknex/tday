<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Allow JIT compilation (required by Electron's V8 engine) -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <!-- Allow unsigned executable memory (required by some native node modules) -->
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <!-- Allow dyld environment variables (required by Electron) -->
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <!-- Disable Library Validation so bundled native modules can load -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <!-- Network client access (needed for local AI model connections) -->
  <key>com.apple.security.network.client</key>
  <true/>
  <!--
    NOTE: Accessibility and Screen Recording do NOT use entitlements.
    They are granted by the user via System Settings (TCC).
    With a proper Developer ID signature, these permissions persist across
    app updates because macOS identifies the app by Team ID + Bundle ID,
    not by binary hash.
  -->
</dict>
</plist>
